# Dev/test/lint dependencies — kept in sync with [dependency-groups.dev] in pyproject.toml.
# Install with: pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
ruff>=0.6
mypy>=1.11
pre-commit>=3.8
